name: Build and Push Docker Images

on:
  push:
    branches:
      - main  
  pull_request:
    branches:
      - main 

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and push Docker image from root directory
        uses: docker/build-push-action@v4
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: |
            your-dockerhub-username/root-image-repo:latest
            your-dockerhub-username/root-image-repo:${{ github.sha }}

      - name: Build and push Docker image from server directory
        uses: docker/build-push-action@v4
        with:
          context: ./server
          file: ./server/Dockerfile
          push: true
          tags: |
            your-dockerhub-username/server-image-repo:latest
            your-dockerhub-username/server-image-repo:${{ github.sha }}

      - name: Post Docker Hub logout
        run: docker logout
